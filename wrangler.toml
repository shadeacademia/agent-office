# Cloudflare Pages — static root + Functions under /functions
# Project name on dash: agent-office-web
name = "agent-office-web"
compatibility_date = "2025-01-01"
pages_build_output_dir = "."

# KV: OFFICE_EVENTS (public status ring for /api/events)
[[kv_namespaces]]
binding = "EVENTS"
id = "1e5ee292417e4c0688fc50b564ccf57c"

# Secret (set in dashboard / API, not in this file):
#   INGEST_TOKEN = long random string (bridge POST only)
